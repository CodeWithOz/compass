# =============================================================================
# Compass — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values.
# The setup script (scripts/setup.sh) does this automatically.
# =============================================================================

# Database — automatically configured by scripts/setup.sh
# Each worktree gets its own Docker Postgres on a unique port.
DATABASE_URL="postgres://compass:compass@localhost:PORT/compass?sslmode=disable"

# AI Providers — fill in at least one
ANTHROPIC_API_KEY=""
OPENAI_API_KEY=""
GOOGLE_API_KEY=""
DEFAULT_AI_PROVIDER="claude"
